// Return Value Practice
// Write a simple function multiply wich accpets two numerical arguments and returns their rpduct (multiply them together). Make sure to return the value instead of printing it!
function multiply(x, y) {
    let multiply = x * y;
    return multiply;
}
// Write a function called isShortsWeather
// - It should accept a single number argument, wich we will call temperature.
//- If temperature is greater or equal to 75, return true. Otherwise return false
function isShortsWeather (temperature) {
    if (temperature >= 75) {
        return true;
    } else {
        return false;
    }
}
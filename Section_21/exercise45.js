// Method Exercise
// Define an object called square, wich will hold methods that have to do the geometry of squares. It should contain two methods, area and perimeter.
//- Ares shoul accept the lenght of a side.
//- Perimeter shoul accpet the lenght of a side and return multiply by 4.
const square = {
    area : function (num) {
        return num * num;
    },
    perimeter : function(num){
        return num * 4;
    }
};